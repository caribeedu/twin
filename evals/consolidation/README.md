# Consolidation / parallel memory evals

Offline contract checks for the Global-Workspace-shaped spine:

- recall silence under the confidence/salience bar
- workspace tick stage order
- consolidation cycle dry-run (never confirms Memory/Judgment)

```bash
python -m evals.consolidation.run
```
